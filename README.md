# Audio-Voice-Removal
This project is a web-based audio processing tool that removes high-frequency noise from audio signals using a Biquad Low-Pass Filter. It allows users to upload audio files, visualize the waveform, apply digital filtering, and download the processed (clean) audio output.
